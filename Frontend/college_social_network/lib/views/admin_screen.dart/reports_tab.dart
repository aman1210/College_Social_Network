import 'package:flutter/material.dart';

class ReportsVerification extends StatelessWidget {
  const ReportsVerification({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: const Text("Reports"),
      ),
    );
  }
}
